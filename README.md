# node-and-react-login-boilerplate


npm install on both server and client directory 

*Add your mongodb atlas uri in .env in /server directory

*Add googleclient id and client secret in /client/config/keys 



to run:

npm run dev
